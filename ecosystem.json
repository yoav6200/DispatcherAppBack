{
    "apps": [
      {
        "name": "DispatcherAppBack", 
        "script": "./app.js", 
        "env": {
          "NODE_ENV": "development" 
        }
      }
    ],
    "deploy": {
      "staging": {
        "key": "/Users/who/.ssh/dispactherKey.pem", 
        "user": "ubuntu",
        "host": ["ec2-51-20-255-87.eu-north-1.compute.amazonaws.com"],
        "ref": "origin/dev", 
        "repo": "git@github.com:yoav6200/DispatcherAppBack.git",
        "path": "/home/ubuntu/DispatcherApp/Back", 
        "ssh_options": "StrictHostKeyChecking=no",
        "post-deploy": "mkdir -p logs && npm install && pm2 reload ecosystem.json --env staging", 
        "pre-deploy-local": "echo 'Deploying code to servers'",
        "env": {
          "NODE_ENV": "staging"
        }
      }
    }
  }
  